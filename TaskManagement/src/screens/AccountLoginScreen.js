import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const AccountLogin = ( {navigation} ) => {
    return(
        <View>
            <Text>This is the AccountLoginScreen</Text>
        </View>
    )
}

const styles = StyleSheet.create({})

export default AccountLogin
